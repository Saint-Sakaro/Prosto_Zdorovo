# Инструкция по применению миграций для модуля maps

## ✅ Миграции созданы

Миграции для модуля `maps` успешно созданы:
- `maps/migrations/0001_initial.py`

## ⚠️ Требуется применить миграции

Для применения миграций нужно:

1. **Убедиться, что PostgreSQL запущен:**
   ```bash
   # Проверить статус PostgreSQL
   brew services list | grep postgresql
   
   # Или запустить если не запущен
   brew services start postgresql
   ```

2. **Применить миграции:**
   ```bash
   python3 manage.py migrate maps
   ```

   Или применить все миграции:
   ```bash
   python3 manage.py migrate
   ```

## 📋 Что будет создано

После применения миграций будут созданы следующие таблицы:

- `maps_poicategory` - категории POI
- `maps_poi` - точки интереса
- `maps_poirating` - рейтинги POI
- `maps_areaanalysis` - результаты анализа областей

А также индексы для оптимизации запросов:
- Индекс по координатам (latitude, longitude)
- Индекс по категории и активности
- Индекс по активности и дате создания
- Индексы для анализа областей

## 🔍 Проверка

После применения миграций можно проверить:

```bash
python3 manage.py showmigrations maps
```

Все миграции должны быть отмечены как `[X]` (применены).

## ⚡ Быстрое решение

Если база данных уже запущена, просто выполните:

```bash
cd /Users/fedorbelov/Documents/Prosto_Zdorovo
python3 manage.py migrate
```

Это применит все непримененные миграции, включая миграции модуля maps.


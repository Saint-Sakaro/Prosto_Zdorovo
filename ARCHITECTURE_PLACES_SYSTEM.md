# Архитектурный план: Система создания мест с динамическими формами

## 📋 Обзор

Система позволяет создавать места двумя способами:
1. **Ручное создание пользователем** - через форму с выбором категории и заполнением полей
2. **Массовая загрузка модератором** - через Excel файл с датасетом

Каждая категория имеет свою схему полей (FormSchema), которая определяет, какие данные нужно заполнить.

---

## 🎯 Этапы реализации

### **ЭТАП 1: Очистка БД и подготовка моделей**

**Цель:** Очистить БД (кроме пользователей) и подготовить модели для новой системы

**Задачи:**
1. Создать скрипт очистки БД (удалить все POI, POICategory, FormSchema, POIRating, но сохранить User)
2. Обновить модель POI:
   - Добавить поле `moderation_status` (pending, approved, rejected)
   - Добавить поле `submitted_by` (ForeignKey to User)
   - Добавить поле `moderated_by` (ForeignKey to User)
   - Добавить поле `moderated_at` (DateTimeField)
   - Добавить поле `moderation_comment` (TextField)
   - Добавить поле `llm_verdict` (JSONField) - для хранения вердикта LLM
3. Создать модель `POISubmission` для хранения заявок на создание мест:
   - Связь с POI (ForeignKey)
   - Статус модерации
   - Данные формы (form_data)
   - Вердикт LLM
   - Комментарий модератора

**Файлы:**
- `maps/models.py` - обновить модель POI
- `maps/management/commands/cleanup_database.py` - скрипт очистки
- `maps/migrations/XXXX_cleanup_and_update_poi.py` - миграция

---

### **ЭТАП 2: Система категорий из Excel**

**Цель:** Создать категории на основе Excel файла и определить поля для каждой категории

**Категории из Excel:**
1. **Точки сбора мусора** (309 записей)
   - Поля: тип точки, форма, адрес, координаты
   - Дополнительные поля: состояние точки, частота вывоза, доступность

2. **Промышленные предприятия** (130 записей)
   - Поля: тип предприятия, описание, адрес, координаты
   - Дополнительные поля: тип производства, экологический класс, наличие фильтров

3. **Предприятия общественного питания** (255 записей)
   - Поля: продажа алкоголя, хорошая реклама, плохая реклама, вердикт
   - Дополнительные поля: тип кухни, наличие вегетарианского меню, гигиенический сертификат

4. **Магазины** (1380 записей)
   - Поля: итог, итог польза, итог вред
   - Дополнительные поля: тип магазина, наличие органических продуктов, наличие алкоголя/табака

5. **Медицинские организации** (1699 записей)
   - Поля: тип организации, код, адрес, координаты
   - Дополнительные поля: специализация, лицензия, режим работы, доступность для инвалидов

**Задачи:**
1. Создать скрипт для анализа Excel и генерации категорий
2. Для каждой категории создать FormSchema с полями
3. Определить веса и направления полей для расчета метрики

**Файлы:**
- `maps/management/commands/import_categories_from_excel.py` - импорт категорий
- `maps/services/category_analyzer.py` - анализ Excel и генерация схем
- `maps/models.py` - категории уже есть, нужно только создать схемы

---

### **ЭТАП 3: Ручное создание места пользователем**

**Цель:** Реализовать создание места через форму с выбором категории и заполнением полей

**Процесс:**
1. Пользователь нажимает "Создать место"
2. Выбирает адрес или ставит метку на карте
3. Выбирает категорию из списка
4. Заполняет поля формы (динамически на основе FormSchema категории)
5. Отправляет заявку
6. Заявка идет на модерацию (статус: pending)

**API эндпоинты:**
- `POST /api/maps/pois/submit/` - создать заявку на место
- `GET /api/maps/pois/submissions/` - список заявок пользователя
- `GET /api/maps/pois/submissions/{id}/` - детали заявки

**Задачи:**
1. Создать serializer для заявки на создание места
2. Создать view для обработки заявок
3. Валидация данных формы на основе FormSchema
4. Автоматический расчет начального рейтинга (S_infra) на основе заполненных полей

**Файлы:**
- `maps/serializers.py` - добавить POISubmissionSerializer
- `maps/views.py` - добавить POISubmissionViewSet
- `maps/services/form_validator.py` - валидация формы
- `maps/services/initial_rating_calculator.py` - расчет начального рейтинга

---

### **ЭТАП 4: Массовая загрузка датасета модератором**

**Цель:** Реализовать загрузку Excel файла модератором

**Процесс:**
1. Модератор загружает Excel файл
2. Система парсит файл и определяет категории
3. Для каждой строки:
   - Если категория существует - проверяет соответствие полей схеме
   - Если категория новая - создает категорию и схему на основе полей
4. Создает POI с данными из Excel
5. Автоматически присваивает статус `approved` (доверяем модератору)
6. Рассчитывает начальный рейтинг

**API эндпоинты:**
- `POST /api/maps/pois/bulk-upload/` - загрузка Excel файла (только для модераторов)

**Задачи:**
1. Создать сервис для парсинга Excel
2. Создать сервис для сопоставления колонок Excel с полями схемы
3. Создать сервис для создания категорий и схем из Excel
4. Обработка ошибок и валидация данных

**Файлы:**
- `maps/services/excel_parser.py` - парсинг Excel
- `maps/services/schema_generator.py` - генерация схем из Excel
- `maps/views.py` - добавить BulkUploadView
- `maps/serializers.py` - добавить BulkUploadSerializer

---

### **ЭТАП 5: Редактор категорий для модераторов**

**Цель:** Создать интерфейс для управления категориями и их схемами

**Функционал:**
1. Список категорий
2. Создание/редактирование категории
3. Редактор схемы (добавление/удаление/редактирование полей)
4. Настройка весов и направлений полей
5. Предпросмотр формы

**API эндпоинты:**
- `GET /api/maps/categories/` - список категорий (уже есть)
- `POST /api/maps/categories/` - создать категорию (только модераторы)
- `PUT /api/maps/categories/{id}/` - обновить категорию
- `GET /api/maps/categories/{id}/schema/` - получить схему
- `PUT /api/maps/categories/{id}/schema/` - обновить схему

**Задачи:**
1. Обновить POICategoryViewSet для поддержки создания/редактирования
2. Создать FormSchemaViewSet для управления схемами
3. Валидация схемы при сохранении

**Файлы:**
- `maps/views.py` - обновить POICategoryViewSet
- `maps/views_ratings.py` - добавить FormSchemaViewSet (или создать отдельный)
- `maps/serializers.py` - добавить FormSchemaSerializer

---

### **ЭТАП 6: Модерация заявок с LLM**

**Цель:** Реализовать модерацию заявок с использованием LLM для предварительной проверки

**Процесс:**
1. При создании заявки пользователем - автоматически отправляется на проверку LLM
2. LLM анализирует:
   - Корректность заполнения полей
   - Соответствие данных категории
   - Потенциальную пользу/вред для здоровья
   - Качество данных
3. LLM возвращает вердикт (approve/reject/review) и комментарий
4. Модератор видит вердикт LLM и принимает решение
5. Модератор может принять/отклонить/запросить изменения

**API эндпоинты:**
- `GET /api/maps/pois/submissions/pending/` - список заявок на модерацию
- `POST /api/maps/pois/submissions/{id}/moderate/` - модерация заявки
- `POST /api/maps/pois/submissions/{id}/llm-check/` - проверка через LLM (ручной запуск)

**Задачи:**
1. Интегрировать LLM сервис для проверки заявок
2. Создать промпты для LLM
3. Обновить модель POI для хранения вердикта LLM
4. Создать view для модерации

**Файлы:**
- `maps/services/llm_moderation_service.py` - сервис модерации через LLM
- `maps/views.py` - добавить модерацию в POISubmissionViewSet
- `maps/services/llm_service.py` - обновить для поддержки модерации

---

### **ЭТАП 7: Метрика начального рейтинга**

**Цель:** Реализовать единую метрику для расчета начального рейтинга места

**Требования:**
1. Метрика должна учитывать:
   - Заполненные поля формы (на основе FormSchema)
   - Веса полей
   - Направления полей (положительное/отрицательное влияние)
   - Категорию места
2. Результат: S_infra (0-100)
3. Метрика рассчитывается автоматически:
   - При создании заявки пользователем
   - При загрузке датасета
   - При подтверждении модератором

**Формула:**
```
Для каждого поля формы:
  s_i = normalize_field_value(field, value)
  Если direction == -1: s_i = 1 - s_i

S_infra_raw = Σ(w_i * s_i) / Σ(|w_i|)
S_infra = min(100, max(0, 100 * S_infra_raw))
```

**Задачи:**
1. Обновить InfrastructureScoreCalculator для работы с form_data
2. Создать сервис для нормализации значений полей
3. Интегрировать расчет в процесс создания места

**Файлы:**
- `maps/services/infrastructure_score_calculator.py` - обновить
- `maps/services/field_value_normalizer.py` - новый сервис
- `maps/signals.py` - добавить сигнал для автоматического расчета

---

## 📊 Структура данных

### POI (обновленная)
```python
- uuid
- name
- category (ForeignKey to POICategory)
- address
- latitude, longitude
- form_schema (ForeignKey to FormSchema)
- form_data (JSONField) - заполненные данные формы
- moderation_status (pending, approved, rejected)
- submitted_by (ForeignKey to User)
- moderated_by (ForeignKey to User)
- moderated_at (DateTimeField)
- moderation_comment (TextField)
- llm_verdict (JSONField) - вердикт LLM
- verified (BooleanField)
- is_active (BooleanField)
```

### POICategory
```python
- uuid
- name
- slug
- description
- form_schema (OneToOne to FormSchema)
- health_weight
- health_importance
- is_active
```

### FormSchema
```python
- uuid
- category (OneToOne to POICategory)
- name
- schema_json (JSONField) - структура полей
- version
- status (draft, approved, archived)
```

### Структура schema_json:
```json
{
  "fields": [
    {
      "id": "field_id",
      "type": "boolean|range|select|photo|text",
      "label": "Название поля",
      "direction": 1 или -1,
      "weight": float,
      "required": boolean,
      "scale_min": float (для range),
      "scale_max": float (для range),
      "mapping": {value: normalized_value} (для select),
      "options": [список значений] (для select)
    }
  ],
  "version": "1.0"
}
```

---

## 🔄 Процесс создания места пользователем

```
1. Пользователь → POST /api/maps/pois/submit/
   {
     "address": "...",
     "latitude": ...,
     "longitude": ...,
     "category_slug": "...",
     "form_data": {...}
   }

2. Backend:
   - Валидирует данные формы на основе FormSchema
   - Создает POI со статусом pending
   - Рассчитывает начальный S_infra
   - Отправляет на проверку LLM (асинхронно)
   - Сохраняет вердикт LLM

3. Модератор → GET /api/maps/pois/submissions/pending/
   - Видит список заявок с вердиктами LLM

4. Модератор → POST /api/maps/pois/submissions/{id}/moderate/
   {
     "action": "approve|reject|request_changes",
     "comment": "..."
   }

5. Backend:
   - Обновляет статус POI
   - Если approved: публикует место, рассчитывает полный рейтинг
   - Если rejected: отправляет уведомление пользователю
```

---

## 🔄 Процесс массовой загрузки

```
1. Модератор → POST /api/maps/pois/bulk-upload/
   - Загружает Excel файл

2. Backend:
   - Парсит Excel
   - Для каждого листа:
     - Определяет категорию (по названию листа или данным)
     - Если категория новая:
       - Создает POICategory
       - Анализирует колонки и создает FormSchema
     - Если категория существует:
       - Проверяет соответствие колонок схеме
     - Для каждой строки:
       - Создает POI с данными из строки
       - Заполняет form_data
       - Статус: approved (доверяем модератору)
       - Рассчитывает S_infra

3. Возвращает результат:
   {
     "total": 100,
     "created": 95,
     "errors": 5,
     "errors_details": [...]
   }
```

---

## 🎯 Приоритеты реализации

1. **Высокий приоритет:**
   - Этап 1: Очистка БД и подготовка моделей
   - Этап 2: Система категорий из Excel
   - Этап 3: Ручное создание места
   - Этап 7: Метрика начального рейтинга

2. **Средний приоритет:**
   - Этап 4: Массовая загрузка датасета
   - Этап 5: Редактор категорий

3. **Низкий приоритет:**
   - Этап 6: Модерация с LLM (можно начать с простой модерации, LLM добавить позже)

---

## 📝 Заметки

1. **Обратная совместимость:** При обновлении модели POI нужно учесть существующие данные
2. **Производительность:** Массовая загрузка должна быть асинхронной (Celery)
3. **Валидация:** Строгая валидация данных формы на основе схемы
4. **Безопасность:** Только модераторы могут загружать датасеты и управлять категориями
5. **Метрика:** Начальный рейтинг должен быть консистентным и воспроизводимым

# Удаление полей health_weight и health_importance из категорий

## ✅ Выполнено

Убраны поля `health_weight` и `health_importance` из модели `POICategory`, так как они больше не нужны.

## 🔄 Причина изменений

После перехода на расчет S_infra через Gigachat:
- **S_infra** уже объективно оценивает влияние конкретного объекта на здоровье
- Gigachat учитывает категорию при оценке
- Дополнительное взвешивание по категориям избыточно

## 📋 Что изменено

### 1. Модель POICategory

**Убраны поля:**
- `health_weight` (FloatField) - вес категории при расчете индекса
- `health_importance` (IntegerField) - важность категории для здоровья

### 2. HealthIndexCalculator

**Упрощен расчет индекса области:**
- Убрано взвешивание по `health_weight` и `health_importance`
- Теперь используется только **фактор надежности** (количество отзывов)
- S_HIS уже объективно отражает влияние объекта

**Новая формула:**
```
object_weight = reliability_factor
health_index = Σ(health_score × reliability_factor) / Σ(reliability_factor)
```

Где `reliability_factor` зависит от количества подтвержденных отзывов.

### 3. Сериализаторы

**POICategorySerializer:**
- Убраны поля `health_weight` и `health_importance` из списка полей

### 4. Админ-панель

**POICategoryAdmin:**
- Убраны поля из `list_display`
- Убран fieldset "Расчет индекса здоровья"

### 5. Фронтенд

**CategoryEditor.tsx:**
- Убраны поля ввода "Вес здоровья" и "Важность здоровья"

**maps.ts:**
- Убраны поля из интерфейса `POICategory`

### 6. Bulk Upload

**BulkUploadPOIView:**
- Убраны значения по умолчанию для `health_weight` и `health_importance` при создании категорий

## 🔧 Миграция

Создана миграция `0003_remove_health_weight_and_importance.py` для удаления полей из БД.

**Применить миграцию:**
```bash
python manage.py migrate maps
```

## ⚠️ Обратная совместимость

- Старые данные с `health_weight` и `health_importance` будут удалены после применения миграции
- Расчет индекса области будет работать по новой упрощенной формуле

## 📊 Новая логика расчета индекса области

Теперь индекс рассчитывается так:
1. Для каждого POI берется его **health_score** (S_HIS)
2. Вес объекта = **надежность** (количество отзывов / 10, минимум 0.1)
3. Объекты с большим количеством отзывов имеют больший вес
4. Итоговый индекс = средневзвешенное значение по надежности

Это более объективно, так как:
- S_infra уже учитывает влияние через Gigachat
- Больше отзывов = более надежная оценка объекта
- Не нужны дополнительные коэффициенты категорий

---

**Статус:** ✅ Готово к использованию
**Дата:** 2025-01-XX

# ⚡ Быстрый старт: Что нужно фронтендеру

## ✅ Главный ответ: НУЖНО ли обновлять фронтенд?

### 🟢 МИНИМАЛЬНЫЕ ИЗМЕНЕНИЯ (5-10 минут)

Фронтенд **будет работать и без изменений**, но для правильной типизации нужно обновить TypeScript типы.

---

## 🔴 ОБЯЗАТЕЛЬНО обновить (типы)

### 1. `frontend/src/api/maps.ts` - добавить новые поля:

```typescript
// В интерфейс POIDetails добавить:
rating: {
  // ... существующие поля ...
  S_infra?: number;      // ⬅️ НОВОЕ
  S_social?: number;     // ⬅️ НОВОЕ  
  S_HIS?: number;        // ⬅️ НОВОЕ
};

// В POIDetails добавить:
form_data?: Record<string, any>;  // ⬅️ НОВОЕ
verified?: boolean;                // ⬅️ НОВОЕ
```

### 2. `frontend/src/api/gamification.ts` - добавить поле rating:

```typescript
// В интерфейс Review добавить:
rating?: number | null;  // ⬅️ НОВОЕ (1-5)
```

---

## 🟡 ЖЕЛАТЕЛЬНО (для полной функциональности)

### 3. Добавить поле оценки в форму отзыва:

В `ReviewForm.tsx` добавить input для rating (1-5) - **опциональное поле**.

### 4. Показать новые компоненты рейтинга:

В `POIModal.tsx` показать S_infra, S_social, S_HIS, если они есть.

---

## 🟢 ОПЦИОНАЛЬНО (можно сделать позже)

- Компонент заполнения анкеты объекта
- Компонент детального просмотра рейтинга
- Админ-панель для схем анкет

---

## ❓ Будет ли работать БЕЗ изменений?

**ДА, будет работать!** Все новые поля опциональны:
- Если полей нет - используется старое поведение
- Если поля есть - можно их отображать

Но TypeScript будет ругаться на типы - поэтому лучше обновить.

---

## 📖 Полное ТЗ

Смотри файл **`FRONTEND_TASK.md`** для детальных инструкций.

---

**Время на обновление:** 5-30 минут (в зависимости от желания)  
**Критичность:** 🟡 Желательно, но не критично


# Реализация массовой загрузки POI (Bulk Upload)

## ✅ Выполнено

Реализован эндпоинт для массовой загрузки POI из Excel файлов.

## 📍 Эндпоинт

**POST** `/api/maps/pois/bulk-upload/`

**Права доступа:** Только модераторы (IsModerator)

## 📋 Формат запроса

### FormData параметры:
- `file` (обязательный) - Excel файл (.xlsx или .xls)
- `auto_create_categories` (опциональный) - автоматически создавать категории, если они не найдены (true/false, по умолчанию false)
- `use_sheet_as_category` (опциональный) - использовать название листа как категорию (true/false, по умолчанию false)
  - Если `true`: каждый лист Excel = одна категория (название листа = название категории)
  - Если `false`: используется один лист с колонкой "категория" (классический режим)

### Формат Excel файла

#### Режим 1: Один лист с колонкой категории (`use_sheet_as_category=false`)

**Обязательные колонки:**
- **название** (или name, cfName) - название POI
- **адрес** (или address, cfAddress) - адрес объекта
- **широта** (или latitude, lat, cfLatitude) - широта координаты (-90 до 90)
- **долгота** (или longitude, lon, lng, cfLongitude) - долгота координаты (-180 до 180)
- **категория** (или category, тип, вид) - название категории

**Опциональные колонки:**
- **описание** (или description, desc) - описание объекта
- **телефон** (или phone, tel) - контактный телефон
- **сайт** (или website, url) - веб-сайт
- **email** (или почта, e-mail) - электронная почта
- **время_работы** (или working_hours, часы_работы) - режим работы
- Любые другие колонки - будут сохранены в `form_data` как дополнительные поля

#### Режим 2: Несколько листов, каждый лист = категория (`use_sheet_as_category=true`)

**Структура файла:**
- Каждый лист Excel = одна категория
- Название листа = название категории
- Все листы обрабатываются автоматически

**Обязательные колонки (для каждого листа):**
- **название** (или name, cfName) - название POI
- **адрес** (или address, cfAddress) - адрес объекта
- **широта** (или latitude, lat, cfLatitude) - широта координаты (-90 до 90)
- **долгота** (или longitude, lon, lng, cfLongitude) - долгота координаты (-180 до 180)

**Опциональные колонки:**
- **описание** (или description, desc) - описание объекта
- **телефон** (или phone, tel) - контактный телефон
- **сайт** (или website, url) - веб-сайт
- **email** (или почта, e-mail) - электронная почта
- **время_работы** (или working_hours, часы_работы) - режим работы
- Любые другие колонки - будут сохранены в `form_data` как дополнительные поля

**Важно:** Колонка "категория" НЕ нужна, т.к. категория определяется по названию листа.

### Пример Excel файла:

| название | адрес | широта | долгота | категория | телефон | описание |
|----------|-------|--------|---------|-----------|---------|----------|
| Аптека №1 | Москва, ул. Ленина, 1 | 55.7558 | 37.6173 | Аптека | +7 (495) 123-45-67 | Круглосуточная аптека |
| Спортзал | Москва, пр. Мира, 10 | 55.7520 | 37.6175 | Спортзал | +7 (495) 234-56-78 | Современный фитнес-центр |

## 📤 Формат ответа

```json
{
  "total": 10,
  "created": 8,
  "errors": 2,
  "errors_details": [
    {
      "row": 3,
      "message": "Широта должна быть от -90 до 90, получено: 95.5"
    },
    {
      "row": 7,
      "message": "Категория \"Несуществующая\" не найдена"
    }
  ],
  "categories_created": [
    "Новая категория 1",
    "Новая категория 2"
  ]
}
```

### Поля ответа:
- `total` - общее количество строк во всех листах
- `created` - количество успешно созданных POI
- `errors` - количество строк с ошибками
- `errors_details` - массив с деталями ошибок (номер строки/лист и сообщение)
- `categories_created` - список автоматически созданных категорий (только если `auto_create_categories=true`)
- `sheets_processed` - количество обработанных листов (только в режиме `use_sheet_as_category=true`)

## 🔧 Особенности реализации

### 1. Автоматическое определение колонок
Система автоматически распознает колонки по различным вариантам названий:
- Название: название, name, имя, наименование, cfName
- Адрес: адрес, address, cfAddress
- Широта: широта, latitude, lat, координата_широта, cfLatitude
- Долгота: долгота, longitude, lon, lng, координата_долгота, cfLongitude
- Категория: категория, category, тип, вид (только в режиме одного листа)
- И т.д.

### 2. Создание категорий
Если категория не найдена и `auto_create_categories=true`:
- Автоматически создается новая категория
- Генерируется slug из названия
- Устанавливаются дефолтные значения (цвет, вес, важность)

### 3. Обработка ошибок
Каждая строка обрабатывается независимо:
- Ошибки в одной строке не останавливают обработку остальных
- Подробные сообщения об ошибках с указанием номера строки
- Все ошибки собираются и возвращаются в ответе

### 4. Статус созданных POI
Все POI, созданные через bulk upload:
- Автоматически получают статус `approved` (одобрено)
- Помечаются как активные (`is_active=True`)
- Помечаются как верифицированные (`verified=True`)
- Привязываются к пользователю, выполнившему загрузку

### 5. Дополнительные поля
Все колонки, не соответствующие стандартным полям, сохраняются в `form_data` как дополнительные данные.

## 🚨 Ошибки и их причины

### Ошибки валидации:
- **Некорректные координаты** - координаты вне допустимого диапазона
- **Отсутствуют обязательные колонки** - в файле нет нужных колонок
- **Некорректный формат файла** - файл не является Excel файлом
- **Категория не найдена** - категория не существует и не разрешено автсоздание

## 📝 Пример использования

### JavaScript/TypeScript (фронтенд):
```typescript
import { bulkUploadPlaces } from './api/places';

// Режим 1: Один лист с колонкой категории
const handleFileUpload = async (file: File) => {
  try {
    const result = await bulkUploadPlaces(file, true, false);
    // auto_create_categories = true, use_sheet_as_category = false
    console.log(`Создано: ${result.created} из ${result.total}`);
    if (result.errors > 0) {
      console.error('Ошибки:', result.errors_details);
    }
  } catch (error) {
    console.error('Ошибка загрузки:', error);
  }
};

// Режим 2: Несколько листов, каждый лист = категория
const handleMultiSheetUpload = async (file: File) => {
  try {
    const result = await bulkUploadPlaces(file, true, true);
    // auto_create_categories = true, use_sheet_as_category = true
    console.log(`Обработано листов: ${result.sheets_processed}`);
    console.log(`Создано: ${result.created} из ${result.total}`);
    if (result.errors > 0) {
      console.error('Ошибки:', result.errors_details);
    }
  } catch (error) {
    console.error('Ошибка загрузки:', error);
  }
};
```

### Python (тестирование):
```python
import requests

url = 'http://localhost:8000/api/maps/pois/bulk-upload/'

# Режим 1: Один лист с колонкой категории
files = {'file': open('places.xlsx', 'rb')}
data = {
    'auto_create_categories': 'true',
    'use_sheet_as_category': 'false'
}
headers = {'Authorization': 'Bearer YOUR_TOKEN'}

response = requests.post(url, files=files, data=data, headers=headers)
print(response.json())

# Режим 2: Несколько листов, каждый лист = категория
data = {
    'auto_create_categories': 'true',
    'use_sheet_as_category': 'true'  # ← ключевой параметр
}

response = requests.post(url, files=files, data=data, headers=headers)
print(response.json())
```

## ✅ Тестирование

Эндпоинт готов к использованию:
- ✅ Синтаксис проверен
- ✅ Django check пройден
- ✅ Импорты корректны
- ✅ Логика реализована

## 📚 Связанные файлы

- `maps/views.py` - класс `BulkUploadPOIView`
- `maps/urls.py` - маршрут `/api/maps/pois/bulk-upload/`
- `maps/models.py` - модели `POI` и `POICategory`

## 🔄 Следующие шаги

1. Протестировать с реальным Excel файлом
2. Оптимизировать для больших файлов (если нужно)
3. Добавить возможность обработки в фоне через Celery (для очень больших файлов)

---

**Статус:** ✅ Готово к использованию

# 📋 Краткое ТЗ для фронтендера

## 🎯 Главное: что нужно обновить?

### ✅ ОБЯЗАТЕЛЬНО (без этого типы будут некорректны)

1. **Обновить TypeScript типы** - добавить новые поля в интерфейсы
2. **Поддержать поле `rating`** в форме создания отзыва

### ⚠️ ВАЖНО (для полной функциональности)

3. **Отображать новые компоненты рейтинга** в детальной карточке POI
4. **Показать верификацию объекта**, если есть

### 🟢 ОПЦИОНАЛЬНО (можно сделать позже)

5. Создать форму заполнения анкеты объекта
6. Добавить компоненты для работы со схемами анкет

---

## 📝 Минимальные изменения (5 минут)

### 1. Обновить типы в `frontend/src/api/maps.ts`

```typescript
// Добавить в интерфейс POIDetails
rating: {
  health_score: number;
  reviews_count: number;
  approved_reviews_count: number;
  S_infra?: number;      // ⬅️ НОВОЕ
  S_social?: number;     // ⬅️ НОВОЕ
  S_HIS?: number;        // ⬅️ НОВОЕ
};

// Добавить в POIDetails
form_data?: Record<string, any>;  // ⬅️ НОВОЕ
verified?: boolean;                // ⬅️ НОВОЕ
```

### 2. Обновить тип Review в `frontend/src/api/gamification.ts`

```typescript
export interface Review {
  // ... существующие поля ...
  rating?: number | null;  // ⬅️ НОВОЕ (1-5)
}
```

### 3. Добавить поле rating в форму отзыва (опционально)

```typescript
// В ReviewForm добавить input для rating (1-5)
// Поле опциональное - отзыв будет работать и без него
```

---

## ❓ Что произойдет, если НЕ обновить?

**Ничего страшного!** 

- Все существующие функции продолжат работать
- Новые поля просто не будут отображаться
- Типы будут некорректными (TypeScript может показывать ошибки)

---

## 📖 Полное ТЗ

См. файл **`FRONTEND_TASK.md`** для детального описания всех изменений.

---

**Статус бэкенда:** ✅ ГОТОВ  
**Обратная совместимость:** ✅ СОХРАНЕНА  
**Срочность обновлений:** 🟡 Желательно, но не критично

